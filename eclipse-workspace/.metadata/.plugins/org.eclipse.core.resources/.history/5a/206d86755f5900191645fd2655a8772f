package pack1;

public class Minimum {
/*Create a class Called Minimumwith in it crate a method called minOfValuesmake it staticFrom your previous
 *  Applicaions class call the method and pass an array argumentand prin out the resultMessage Input
 */
	
	static int  minOfValues(int[]x) {
		int min=Integer.MAX_VALUE;
		int max=x[0];
		for(int y:x) {
			if (y>max) {
				max=y;
			}
		}
		
		
		return max;
 
		
	}
	
	

}
